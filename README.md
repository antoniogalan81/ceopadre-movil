# CEOPadre · interfaz móvil

Sólo cliente. La clave incluida es la pública del proyecto Supabase: sin iniciar sesión no lee ni una fila (RLS). Se genera desde el repositorio privado de CEOPadre con `tools/publish-mobile.mjs`; no se edita a mano.
